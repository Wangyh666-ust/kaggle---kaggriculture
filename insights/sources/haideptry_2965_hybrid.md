# 2965+ Master Harvest（haideptry）—— **机制我们已有，实测我们赢 90%**

- **链接**：https://www.kaggle.com/code/haideptry/the-2965-master-hybrid-engine
- **产物**：sha256 **`cda529cb6b359fc4…`**，1,045,228 B / 7,375 行
- **Kaggle 入口**：`v7_hr_agent`
- **性质**：**新的**（本地此前没有这份 agent），已收进 `opponents/hybrid2965/`

## 一、它的主张

> "**V59 Harvest Ledger + Protected Order Book**……回合内订单簿求值器：**搜索现金卖单槽位的排列**，
> 同时**保持买入与雇工序列锁定**。把对手建模成主动的镜像玩家，抢在价格衰减之前成交。
> 比旧的 2950 基线每局 **+$928**（Seed 102），压制旧 V54 超过 **+$1,800/局**。"

它给的对手表：对 `cha22` **+$274/局**（Seed 101）、对旧 2950 **+$928**（Seed 102）、
对 V54 **100%**、对 **ahmed v56/v57 92% 胜率 +$360/局**、对 Metav4 v13 **96%**、对 V50 **100%**。

⚠️ **每一个数字都只标了一个种子**（Seed 101 / 102），或者干脆没标。**n=1~2 局级别的证据。**

## 二、它的核心机制 = 我们 v41 的 `_CXD`

它描述的"**回合内搜索现金卖单槽位排列、买入/雇工锁定**"与我们 v41 移植的 `_CXD`（F4 "Layer D"）
**是同一个机制**：`_CXD` 枚举 SELL 与固定价订单所占据位的排列，取最坏情况 margin 最好的那个，
不新造、不跨回合移动任何市场意图。

### 层标记逐项对照（两边都是 grep 计数）

| 标记 | 它 | 我们 `main.py` | 说明 |
|---|---|---|---|
| `_CXD_` | **20** | **0**（在 `experiments/v41/cxd.py` 里） | 同一机制 |
| `_MPX_` | **17** | **0**（在 `experiments/v40/mpx.py` 里） | 同一机制 |
| `_ADV_LOOK` | **0** | **2** | **它没有 ADV 层，我们有** |
| `_IG_` | 30 | 16 | 它的 IG 层更多 |
| `_E402` | 24 | 12 | |
| `_E410` | 7 | 6 | |
| `_V231` / `_V233` | 12 / 28 | **12 / 28** | **逐项相同**（prvsiyan 的层，我们都有） |
| `_V92_P_EVERY` / `make_agent` / `chassis` | 2 / 2 / 103 | 2 / 2 / 106 | 同一底盘 |

**⇒ 共同底盘 + 双方都收敛到了 `_CXD` 与 `_MPX`；差别在于它没有 `_ADV_*` 而我们没有它那多出来的 IG/E402 副本。**

## 三、我们的实测（40 局，种子 1000-1019 双座位）

| | |
|---|---|
| 战绩 | **36 胜 / 4 负 = 我们赢 90%** |
| 均差 | **+$650** |
| 最差单局 | −$622 |

它是**我们测过的近期公开 agent 里最强的**——其余多是 40-0（`pipe7`/`tetsu_market` 100%、
`pipe18`/`v55`/`v56`/`v57` 93–100%），只有 `tetsutani_cha22` 是同级（55%）。
**但它仍然输给我们 90-10，而且均差只有 +$650（其他对手是 +$1,700~+$4,400）** ⇒ 它确实更接近我们，但不是同级。

### 一个可以对比的数字

| 对象 | 它声称 | 我们实测 |
|---|---|---|
| 对 ahmed v56/v57 | 92% 胜率、+$360/局（未标种子数） | **我们 v41 对 v56+v57 = 96.0%**（400 局、两块独立种子） |

**同一个对手集合，我们的实测比它的声称更好，而且样本量大两个数量级。**

## 四、结论与待办

| # | 结论 |
|---|---|
| 1 | **机制没有新东西**：`_CXD` + `_MPX` 我们已经有了（v40/v41），而且是用 800 局两块种子验收的 |
| 2 | **证据标准照旧**：它的全部数字都是单种子级；这与 G9 一致 |
| 3 | **值得收进对手池**：它是目前最接近我们的公开 agent，可以当作除 `tetsutani_cha22` 之外的**第二个有用的验收对手** |
| 4 | 🔶 **唯一可能的差异线索**：它没有 `_ADV_LOOK`，而我们有（v44 刚提到 6）。方向上与我们 v44 的结论一致——但那仍然只是**两条种子块 +6pp** 的证据，别过度解读 |
| 5 | 🔶 它多出来的 `_IG_`/`_E402` 副本是否有效？可以做一个"把它的 IG 层搬过来"的实验，但它对我们 90-10 落后，**优先级低于 step-1 挤压** |
